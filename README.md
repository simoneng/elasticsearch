# elasticsearch

Before using http.py:
a) download the file favicon.ico into same directory/folder as http.py
b) rename the file from http.py to http2.py (this avoids conflict with Python module with the same name "http")
